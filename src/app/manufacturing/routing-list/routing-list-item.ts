export class RoutingListItem {
    constructor(
        public id: number,
        public code: string,
        public name: string,
        public created: Date,
        public lastUpdated: Date
    ) {        
    }
}